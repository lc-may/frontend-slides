# HTML Template Reference

Complete HTML architecture for frontend slides presentations.

---

## Basic Structure

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Presentation Title</title>

    <!-- Fonts (use Fontshare or Google Fonts) -->
    <link rel="stylesheet" href="https://api.fontshare.com/v2/css?f[]=...">

    <style>
        /* CSS here - see CSS Custom Properties section */
    </style>
</head>
<body>
    <!-- Progress bar (optional) -->
    <div class="progress-bar"></div>

    <!-- Navigation dots (optional) -->
    <nav class="nav-dots">
        <!-- Generated by JS -->
    </nav>

    <!-- Slides -->
    <section class="slide title-slide">
        <h1 class="reveal">Presentation Title</h1>
        <p class="reveal">Subtitle or author</p>
    </section>

    <section class="slide">
        <h2 class="reveal">Slide Title</h2>
        <p class="reveal">Content...</p>
    </section>

    <!-- More slides... -->

    <script>
        /* JavaScript here - see scripts/slide-controller.js */
    </script>
</body>
</html>
```

---

## CSS Custom Properties (Theme)

Easy to modify: change these to change the whole look:

```css
:root {
    /* Colors */
    --bg-primary: #0a0f1c;
    --bg-secondary: #111827;
    --text-primary: #ffffff;
    --text-secondary: #9ca3af;
    --accent: #00ffcc;
    --accent-glow: rgba(0, 255, 204, 0.3);

    /* Typography - MUST use clamp() for responsive scaling */
    --font-display: 'Clash Display', sans-serif;
    --font-body: 'Satoshi', sans-serif;
    --title-size: clamp(2rem, 6vw, 5rem);
    --subtitle-size: clamp(0.875rem, 2vw, 1.25rem);
    --body-size: clamp(0.75rem, 1.2vw, 1rem);

    /* Spacing - MUST use clamp() for responsive scaling */
    --slide-padding: clamp(1.5rem, 4vw, 4rem);
    --content-gap: clamp(1rem, 2vw, 2rem);

    /* Animation */
    --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
    --duration-normal: 0.6s;
}
```

---

## Base Styles

```css
/* ===========================================
   BASE STYLES
   =========================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    scroll-snap-type: y mandatory;
    height: 100%;
}

body {
    font-family: var(--font-body);
    background: var(--bg-primary);
    color: var(--text-primary);
    overflow-x: hidden;
    height: 100%;
}
```

---

## Slide Container Styles

```css
/* ===========================================
   SLIDE CONTAINER
   CRITICAL: Each slide MUST fit exactly in viewport
   - Use height: 100vh (NOT min-height)
   - Use overflow: hidden to prevent scroll
   - Content must scale with clamp() values
   =========================================== */
.slide {
    width: 100vw;
    height: 100vh; /* EXACT viewport height - no scrolling */
    height: 100dvh; /* Dynamic viewport height for mobile */
    padding: var(--slide-padding);
    scroll-snap-align: start;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden; /* Prevent any content overflow */
}

/* Content wrapper that prevents overflow */
.slide-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    max-height: 100%;
    overflow: hidden;
}
```

---

## Typography

```css
/* ===========================================
   TYPOGRAPHY
   =========================================== */
h1, h2, h3 {
    font-family: var(--font-display);
    font-weight: 700;
    line-height: 1.1;
}

h1 {
    font-size: var(--title-size);
    margin-bottom: var(--content-gap);
}

h2 {
    font-size: var(--h2-size, clamp(1.25rem, 3.5vw, 2.5rem));
    margin-bottom: clamp(0.5rem, 1.5vw, 1rem);
}

h3 {
    font-size: var(--h3-size, clamp(1rem, 2.5vw, 1.75rem));
}

p, li {
    font-size: var(--body-size);
    line-height: 1.6;
    color: var(--text-secondary);
}

/* Strong/emphasis */
strong {
    color: var(--text-primary);
    font-weight: 600;
}
```

---

## Animation Styles

```css
/* ===========================================
   ANIMATIONS
   Trigger via .visible class (added by JS on scroll)
   =========================================== */
.reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity var(--duration-normal) var(--ease-out-expo),
                transform var(--duration-normal) var(--ease-out-expo);
}

.slide.visible .reveal {
    opacity: 1;
    transform: translateY(0);
}

/* Stagger children */
.reveal:nth-child(1) { transition-delay: 0.1s; }
.reveal:nth-child(2) { transition-delay: 0.2s; }
.reveal:nth-child(3) { transition-delay: 0.3s; }
.reveal:nth-child(4) { transition-delay: 0.4s; }
.reveal:nth-child(5) { transition-delay: 0.5s; }
.reveal:nth-child(6) { transition-delay: 0.6s; }
```

---

## Progress Bar

```css
/* ===========================================
   PROGRESS BAR
   =========================================== */
.progress-bar {
    position: fixed;
    top: 0;
    left: 0;
    height: 3px;
    background: var(--accent);
    z-index: 1000;
    transition: width 0.3s ease;
    box-shadow: 0 0 10px var(--accent-glow);
}
```

---

## Navigation Dots

```css
/* ===========================================
   NAVIGATION DOTS
   =========================================== */
.nav-dots {
    position: fixed;
    right: 2rem;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    z-index: 1000;
}

.nav-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--text-secondary);
    border: none;
    cursor: pointer;
    opacity: 0.4;
    transition: all 0.3s ease;
}

.nav-dot:hover {
    opacity: 0.7;
    transform: scale(1.2);
}

.nav-dot.active {
    opacity: 1;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent-glow);
}

@media (max-width: 768px) {
    .nav-dots {
        right: 1rem;
    }
}
```

---

## Common Slide Types

### Title Slide

```html
<section class="slide title-slide">
    <div class="slide-content">
        <h1 class="reveal">Presentation Title</h1>
        <p class="reveal">Subtitle or tagline</p>
        <p class="reveal author">Author Name • Date</p>
    </div>
</section>
```

### Content Slide with Bullets

```html
<section class="slide">
    <div class="slide-content">
        <h2 class="reveal">Section Title</h2>
        <ul class="reveal bullet-list">
            <li>First key point</li>
            <li>Second key point</li>
            <li>Third key point</li>
            <li>Fourth key point</li>
        </ul>
    </div>
</section>
```

### Feature Grid

```html
<section class="slide">
    <div class="slide-content">
        <h2 class="reveal">Features</h2>
        <div class="grid reveal">
            <div class="card">
                <h3>Feature 1</h3>
                <p>Description</p>
            </div>
            <div class="card">
                <h3>Feature 2</h3>
                <p>Description</p>
            </div>
            <div class="card">
                <h3>Feature 3</h3>
                <p>Description</p>
            </div>
        </div>
    </div>
</section>
```

### Quote Slide

```html
<section class="slide">
    <div class="slide-content">
        <blockquote class="reveal">
            "The quote text goes here. Keep it to 2-3 lines maximum."
        </blockquote>
        <cite class="reveal">— Attribution, Title</cite>
    </div>
</section>
```

### Image Slide

```html
<section class="slide">
    <div class="slide-content">
        <h2 class="reveal">Image Title</h2>
        <div class="image-container reveal">
            <img src="assets/image.jpg" alt="Description">
        </div>
    </div>
</section>
```

---

## Accessibility Requirements

Always include:

```css
/* Focus styles for keyboard navigation */
:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
}

/* Screen reader only content */
.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    border: 0;
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
    .reveal {
        transition: opacity 0.3s ease;
        transform: none;
    }
}
```

Use semantic HTML:

```html
<section class="slide" aria-label="Slide 1: Introduction">
    <h1>Title</h1>
    <!-- content -->
</section>

<nav class="nav-dots" aria-label="Slide navigation">
    <!-- dots -->
</nav>
```

---

## Code Quality Requirements

### Comments

Every section should have clear comments explaining:

```css
/* ===========================================
   SECTION NAME
   Brief description of what this does.
   - Key point 1
   - Key point 2
   =========================================== */
```

```javascript
/**
 * FUNCTION NAME
 * Brief description.
 *
 * @param {type} name - Description
 * @returns {type} Description
 */
```

### Responsive Checklist

- [ ] All font sizes use `clamp()`
- [ ] All spacing uses `clamp()` or viewport units
- [ ] Breakpoints for heights: 700px, 600px, 500px
- [ ] Breakpoint for narrow width: 600px
- [ ] Mobile touch interactions work
- [ ] Keyboard navigation works
- [ ] Reduced motion respected
